<Window x:Class="DongNoti.Views.MissedAlarmsSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="집중 모드 종료" Height="500" Width="550"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/app.ico"
        ResizeMode="CanResize"
        MinHeight="400"
        MinWidth="450"
        Background="Transparent"
        AllowsTransparency="False">
    <Window.Resources>
        <Style x:Key="ModernRaisedButton" TargetType="Button">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="100"/>
            <Setter Property="MinHeight" Value="36"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect Color="#40000000" Direction="270" ShadowDepth="2" BlurRadius="4" Opacity="0.3"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1565C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="8" Padding="28" Margin="8">
        <Border.Effect>
            <DropShadowEffect Color="#20000000" Direction="270" ShadowDepth="2" BlurRadius="12" Opacity="0.2"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 제목 -->
            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="🔔" 
                           FontSize="48"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,12"/>
                <TextBlock Text="집중 모드가 종료되었습니다" 
                           FontSize="22" 
                           FontWeight="Bold"
                           Foreground="#212121"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,8"/>
                <TextBlock x:Name="SubtitleText"
                           Text="집중하는 동안 놓친 알람이 있습니다" 
                           FontSize="14" 
                           Foreground="#757575"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>
            </StackPanel>

            <!-- 구분선 -->
            <Separator Grid.Row="1" Margin="0,0,0,20" Background="#E0E0E0"/>

            <!-- 통계 -->
            <Border Grid.Row="2" 
                    Background="#FFF3E0"
                    BorderBrush="#FFB74D"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="16,12"
                    Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock Text="📊" 
                               FontSize="18"
                               VerticalAlignment="Center"
                               Margin="0,0,12,0"/>
                    <TextBlock x:Name="StatisticsText"
                               Text="총 5개의 알람을 놓치셨습니다"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="#E65100"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <!-- 놓친 알람 목록 -->
            <ScrollViewer Grid.Row="3" 
                          VerticalScrollBarVisibility="Auto"
                          Margin="0,0,0,20">
                <ItemsControl x:Name="MissedAlarmsListBox">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White"
                                    BorderBrush="#E0E0E0"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="16,12"
                                    Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- 아이콘 -->
                                    <TextBlock Text="⏰" 
                                               FontSize="20"
                                               VerticalAlignment="Center"
                                               Margin="0,0,12,0"/>
                                    
                                    <!-- 제목 및 반복 정보 -->
                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Title}" 
                                                   FontSize="14"
                                                   FontWeight="SemiBold"
                                                   Foreground="#212121"
                                                   TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding RepeatTypeString}" 
                                                   FontSize="11"
                                                   Foreground="#9E9E9E"
                                                   Margin="0,2,0,0"/>
                                    </StackPanel>
                                    
                                    <!-- 시간 -->
                                    <TextBlock Grid.Column="2"
                                               Text="{Binding ScheduledTime, StringFormat=HH:mm}" 
                                               FontSize="15"
                                               FontWeight="Bold"
                                               Foreground="#2196F3"
                                               VerticalAlignment="Center"
                                               Margin="12,0,0,0"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>

            <!-- 확인 버튼 -->
            <Button Grid.Row="4" 
                    Content="확인" 
                    Click="OK_Click" 
                    IsDefault="True"
                    HorizontalAlignment="Center"
                    Style="{StaticResource ModernRaisedButton}"
                    MinWidth="150"/>
        </Grid>
    </Border>
</Window>
